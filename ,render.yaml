services:
  - type: web
    name: python-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn flask_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10